# Frontend → backend connection (server-side only, never exposed to browser)
# BACKEND_URL is an internal runtime var (set by entrypoint.sh in the stack).
BACKEND_URL=http://localhost:8080
CRONJOB_MANAGER_API_SECRET=change-me-super-secret
